name: ils_painter
description: A simple widget to paint with your fingers. Supports setting a background color, undo and export to png!
version: 0.0.1+1
homepage: https://github.com/iLeafSolutionsPvtLtd/ILSPainter

dependencies:
  flutter:
    sdk: flutter
  json_serializable: ^6.5.1
  json_annotation: ^4.7.0
dev_dependencies:
  build_runner:

environment:
  sdk: '>=2.18.1 <4.0.0'
